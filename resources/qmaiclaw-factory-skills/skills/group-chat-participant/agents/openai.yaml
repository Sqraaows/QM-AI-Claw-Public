interface:
  display_name: "群聊参与分寸"
  short_description: "判断在群聊中何时回应、何时沉默，做到有价值但不打扰。"
  default_prompt: "Use $group-chat-participant to decide whether to reply in a group chat."

policy:
  allow_implicit_invocation: true